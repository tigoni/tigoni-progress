---
import { markdownToHtml } from "@/lib/markdown";

interface Props {
  className?: string;
  content: string;
}

const { className, content } = Astro.props;
---

<div class:list={["prose prose-neutral dark:prose-invert max-w-none", className]} set:html={markdownToHtml(content)} />
