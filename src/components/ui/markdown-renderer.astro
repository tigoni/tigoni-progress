---
import { markdownToHtml } from "@/lib/markdown";
import { cn } from "@/lib/utils";

interface Props {
  className?: string;
  content: string;
}

const { className, content } = Astro.props;
---

<p class={cn("prose", className)} set:html={markdownToHtml(content)} />
